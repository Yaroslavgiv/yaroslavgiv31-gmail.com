import 'package:flutter/material.dart';

import 'cart_screen.dart';

class ScreenTwo extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Оформления чека"),
      ),
      body: Center(
        child: Text("Seckond"
        ),
      ),
    );
  }
}
